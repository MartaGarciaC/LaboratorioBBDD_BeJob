let consulta = document.getElementById('consultar');

consulta.addEventListener('click', datos);

function datos() {
  let lista = document.getElementById('lista');
  lista.style.visibility='visible';
}